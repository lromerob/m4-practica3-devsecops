name: Pipeline DevSecOps CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    name: Build and Scan Docker Image
    runs-on: ubuntu-latest

    steps:
    - name: Checkout de Código
      uses: actions/checkout@v3

    - name: Configurar Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Construir Imagen Docker
      uses: docker/build-push-action@v4
      with:
        context: .
        push: false
        tags: lromerob/app-flask:latest

    # NUEVO PASO: Escaneo de seguridad con Trivy
    - name: Escanear Imagen con Trivy
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'lromerob/app-flask:latest'
        format: 'table'
        exit-code: '0' # Ponemos 0 para que no rompa el pipeline si encuentra fallos, así podemos ver el reporte completo primero
        severity: 'CRITICAL,HIGH'
